apply plugin: 'com.android.application'

android {
    signingConfigs {
        release_medval {
            keyAlias 'medvalmobile'
            keyPassword '21dejulio2010'
            storeFile file('/Users/Supertel/Dropbox/MOOC/medvalmobile.keystore')
            storePassword 'e2r8i9c8k7'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "ec.medval.hackatoniee"
        minSdkVersion 14
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.google.android.gms:play-services:6.5.87'
}
